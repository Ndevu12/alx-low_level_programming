#include "main.h"

/**
 * add - it will add two numbers
 * @a: value to be added
 * @b: value to be added
 * Return: value
 */
int add(int a, int b)
{
	int sum = 0;
	/*lets sum up two numbers*/
	sum = a + b;
	return (sum);
}
