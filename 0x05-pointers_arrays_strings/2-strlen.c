#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: length of a string.
 * Return: always nothing
 */
int _strlen(char *s)
{
return (strlen(s));
}
