#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ra;
/*Every thing is possible when you are patient and persistant*/
	for (ra = 'z'; ra >= 'a'; ra--)
	{
		putchar(ra);
	}
	putchar('\n');
	return (0);
}
