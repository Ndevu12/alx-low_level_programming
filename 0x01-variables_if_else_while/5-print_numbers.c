#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num;
	/*This line stands for nothing*/
	for (num = 0; num < 10; num++)
	{
		printf("%d\n", num);
	}
	return (0);
}
