#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int nu;
/*This means nothing about this code*/
	for (nu = 0; nu <= 9; nu++)
	{
		putchar('0' + nu);
	}
	putchar('\n');
	return (0);
}
